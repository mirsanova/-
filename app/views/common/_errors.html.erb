  <% if resource.errors.any? %>
    <div id="error_explanation" class="alert alert-danger col-md-4 col-md-offset-4" role="alert">
      <h4 class=text-center>
        <%= pluralize(resource.errors.count, "error") %> prohibited <br>
        this from being saved:
      </h2>
      <ul>
        <% resource.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

